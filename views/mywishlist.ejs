<div class="container mt-5">
    <h2 class="mb-4">My Wishlist</h2>
  
    <% if (listings.length === 0) { %>
      <p>You haven't saved any properties yet.</p>
    <% } %>
  
    <div class="row card-click">
      <% listings.forEach(listing => { %>
        <div class="col-md-4 mb-4">
          <div class="card shadow">
            <img src="<%= listing.image.url %>" 
                 class="card-img-top" 
                 alt="<%= listing.title %>">
  
            <div class="card-body">
              <h5 class="card-title"><%= listing.title %></h5>
              <p class="card-text">
                ₹ <%= listing.price.toLocaleString("en-IN") %> / night
              </p>
  
              <a href="/property/<%= listing.id %>" 
                 class="btn btn-primary">
                View Property
              </a>
            </div>
          </div>
        </div>
      <% }) %>
    </div>
  </div>
  

  <!-- <script>
    // Select all cards and attach click event listeners
    document.querySelectorAll('.card-click').forEach(card => {
      card.addEventListener('click', () => {
        const id = card.getAttribute('data-id');
        // console.log('Card clicked with ID:', id);
        // Optional: navigate to the property page
        window.location.href = `profile/wishlist/property/${id}`;
      });
    });
  </script> -->